"use client";

import { useEffect, useState } from "react";
import { useParams } from "next/navigation";

const NAVY = "#071B34";
const ORANGE = "#F97316";

export default function VehicleDetailsPage() {
  const params = useParams(); const [vehicle, setVehicle] = useState(null); const [sheets, setSheets] = useState([]);
  useEffect(() => { const vehicles = JSON.parse(window.localStorage.getItem("agc_vehicles") || "[]"); const loadingSheets = JSON.parse(window.localStorage.getItem("agc_loading_sheets") || "[]"); const item = vehicles.find((record) => record.id === decodeURIComponent(params?.id || "")); setVehicle(item || null); setSheets(loadingSheets.filter((sheet) => sheet.truckNumber === item?.truckNumber).reverse()); }, [params]);
  if (!vehicle) return <main className="flex min-h-screen items-center justify-center bg-slate-50"><div className="rounded-2xl bg-white p-8 text-center shadow-sm"><h1 className="text-xl font-bold" style={{ color: NAVY }}>Vehicle not found</h1><a href="/vehicles" className="mt-4 inline-flex rounded-lg px-4 py-2 text-sm font-semibold text-white" style={{ backgroundColor: ORANGE }}>Back to Vehicles</a></div></main>;
  const expiry = ["insuranceExpiry", "fitnessExpiry", "permitExpiry", "pollutionExpiry"].filter((field) => vehicle[field] && vehicle[field] <= new Date(Date.now() + 30 * 86400000).toISOString().slice(0, 10));
  return <main className="min-h-screen bg-slate-50 px-4 py-6 sm:px-6 lg:px-8"><div className="mx-auto max-w-6xl"><div className="mb-6 flex items-end justify-between"><div><a href="/vehicles" className="text-xs font-semibold uppercase tracking-[0.14em] text-slate-500">Vehicle Master / Details</a><h1 className="mt-2 text-3xl font-bold" style={{ color: NAVY }}>{vehicle.truckNumber}</h1><p className="mt-1 text-sm text-slate-500">{vehicle.id} · {vehicle.vehicleType || "Vehicle"}</p></div><div className="flex gap-2"><a href={`/vehicles/${vehicle.id}?edit=true`} className="rounded-lg border border-slate-200 bg-white px-4 py-2 text-sm font-semibold text-slate-700">Edit Vehicle</a><a href="/loading/new" className="rounded-lg px-4 py-2 text-sm font-semibold text-white" style={{ backgroundColor: ORANGE }}>Create Loading Sheet</a></div></div><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="mb-4 text-sm font-bold uppercase tracking-[0.12em]" style={{ color: NAVY }}>Vehicle Information</h2><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Truck Number", vehicle.truckNumber], ["Vehicle Type", vehicle.vehicleType], ["Capacity", vehicle.capacity], ["Route", vehicle.route], ["Current Status", vehicle.status || "Active"], ["Insurance Expiry", vehicle.insuranceExpiry], ["Fitness Expiry", vehicle.fitnessExpiry], ["Permit Expiry", vehicle.permitExpiry], ["Pollution Expiry", vehicle.pollutionExpiry]].map(([label, value]) => <div key={label}><p className="text-[10px] font-bold uppercase tracking-[0.1em] text-slate-400">{label}</p><p className="mt-1 text-sm font-semibold text-slate-800">{value || "-"}</p></div>)}</div></section><section className="mt-4 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="mb-4 text-sm font-bold uppercase tracking-[0.12em]" style={{ color: NAVY }}>Driver Information</h2><div className="grid gap-4 sm:grid-cols-3">{[["Driver Name", vehicle.driverName], ["Driver Mobile", vehicle.driverMobile], ["License Number", vehicle.licenseNumber]].map(([label, value]) => <div key={label}><p className="text-[10px] font-bold uppercase tracking-[0.1em] text-slate-400">{label}</p><p className="mt-1 text-sm font-semibold text-slate-800">{value || "-"}</p></div>)}</div></section><div className="mt-4 grid gap-4 sm:grid-cols-3">{[["Active Trip", sheets.find((sheet) => sheet.status !== "Dispatched")?.manifestNumber || "None"], ["Recent Loading Sheets", sheets.length], ["Upcoming Expiry Alerts", expiry.length]].map(([label, value]) => <section key={label} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-[10px] font-bold uppercase tracking-[0.1em] text-slate-400">{label}</p><p className="mt-2 text-2xl font-bold" style={{ color: NAVY }}>{value}</p></section>)}</div><section className="mt-4 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="mb-4 text-sm font-bold uppercase tracking-[0.12em]" style={{ color: NAVY }}>Recent Loading Sheets</h2>{sheets.slice(0, 10).map((sheet) => <a key={sheet.manifestNumber} href={`/loading/${sheet.manifestNumber}`} className="flex justify-between border-b border-slate-100 py-3 text-sm"><span className="font-semibold" style={{ color: NAVY }}>{sheet.manifestNumber}</span><span className="text-slate-500">{sheet.date || "-"} · {sheet.status}</span></a>)}{!sheets.length && <p className="text-sm text-slate-500">No loading sheets found.</p>}</section></div></main>;
}
